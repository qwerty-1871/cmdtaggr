#!/bin/bash

tgdr=~/.cmdtaggr/tags # The directory where tags are stored
keepfl=0 # Whether or not to keep the list of files in a tag. 0 means it will be deleted and 1 means it will be kept. If kept, it will be moved to the subfolder 'fl' in your tags directory and given the name of the corresponding tag.
